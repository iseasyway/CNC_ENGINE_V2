# -*- coding: utf-8 -*-
"""

============================================================
🏭 C_2 工廠：圓心

============================================================
"""

import math


def solve_c_2(data):


    # ==================================================
    # 1️⃣ 讀取並整理輸入（對齊 Excel B 系列）
    # --------------------------------------------------
    # 說明：
    # - 所有輸入皆轉為 float
    # - 外徑一律轉為半徑計算
    # ==================================================
    theta_deg  = float(data["角度2"]) 
    r  = float(data["刀鼻半徑"])

   

    theta = math.radians(theta_deg)

    X = r * math.sin(theta)
    Z = r * math.cos(theta)

    def _fmt(x):
    # 例如固定顯示到小數點兩位
        return f"{x:.3f}"
    C53=X
    C54=Z
    result_values = {
        "C53": C53,
        "C54": C54,
  
     }

    text_lines = [
        "➡️ 前端斜角 → 平線 → 終點弧（C 工廠）",
        f"C53 = {_fmt(C53)}",
        f"C54 = {_fmt(C54)}",
      
    ]

    return {
        "ok": True,
        "text_lines": text_lines,
        "values": result_values,
    }


# -------------------------------------------------------
# 🔁 對外統一介面
# -------------------------------------------------------
def solve(data: dict):
    return solve_c_2(data)


